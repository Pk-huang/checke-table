# 任務 Checklist

## 狀態說明

- `[x]` 已完成並驗證
- `[ ]` 尚未完成
- `[~]` 已有雛形，但仍需補齊或接入真實流程

## 目前原則

- 不修改 `server.py`。
- 先讓前後端真實串接可運作，再補齊支線、README 與優化。
- 每完成一個小段落，優先跑最窄的驗證，例如 `npm test` 或 `npm run build`。
- 做不到或決定不做的部分，要在 README 說明原因與後續方向。

## 第一階段交付門檻

- [x] 建立 React + TypeScript + Tailwind 前端專案。
- [x] 將前端加入 `docker-compose.yml`。
- [x] 建立上傳、解析中、審核、完成的 mock UI 流程。
- [x] 建立欄位資料模型，涵蓋必填、低信心、多候選與頁碼。
- [x] 建立欄位分組工具，依基本資料、營養標示、檢驗結果、廠商資訊呈現。
- [x] 建立必填欄位驗證工具。
- [x] 補上至少一支測試，先保護必填欄位缺漏規則。
- [x] 確認 `npm test` 通過。
- [x] 確認 `npm run build` 通過。

## 下一階段：前後端串接

- [x] 將 `frontend/src/api/documents.ts` 改為呼叫 `POST /api/documents`。
- [x] 上傳成功後保存後端回傳的 `document_id` 與 `filename`。
- [x] 將 `frontend/src/api/extractStream.ts` 改為讀取 SSE endpoint。
- [x] 支援 `stage` event，更新解析階段與進度。
- [x] 支援 `field` event，一筆一筆累積解析欄位。
- [x] 支援 `done` event，完成後進入審核畫面。
- [x] 支援 `error` event，停止 loading 並顯示可恢復錯誤狀態。
- [x] 取消解析時使用 `AbortController` 停止前端等待。
- [x] 取消後忽略晚到的 `field` 或 `done` event。

## 審核流程補齊

- [x] 欄位可編輯。
- [x] 必填缺漏會阻擋送出。
- [x] 低信心欄位顯示「建議檢查」。
- [x] 多候選欄位可選候選值或改為自訂輸入。
- [x] 審核頁改吃真實 SSE 回傳欄位，而不是固定 mock data。
- [x] 確認同一群組內保留後端回傳的相對順序。
- [x] 確認大量欄位時仍能透過群組掃描與操作。

## 測試與驗證

- [x] 測試必填欄位缺漏規則。
- [x] 測試上傳 API adapter 的輸入與輸出轉換。
- [x] 測試 SSE event 解析與 split chunk 處理。
- [x] 測試欄位分組順序與群組內相對順序。
- [~] 視時間補測解析失敗不會永久 loading。
- [x] 手動驗證 `docker compose up` 後前後端可開啟。
- [x] 手動驗證預設解析流程可從上傳走到審核。
- [x] 使用 `fail_at=3` 驗證解析失敗狀態。
- [x] 使用 `field_count=120` 或 `field_count=300` 驗證大量欄位。
- [x] 使用取消按鈕驗證解析中止後不會誤進審核頁。

## README 交付內容

- [x] 說明如何啟動與驗證專案。
- [x] 說明實際完成的功能。
- [x] 說明決定不做或延後的項目與原因。
- [x] 回答 AI 參考圖最嚴重的三個問題，以及本版本如何處理。
- [x] 回答需求不清楚處與採用的假設。
- [x] 說明介面中哪些資訊被降級顯示或不顯示。
- [x] 說明兩條互相衝突的需求與取捨。
- [x] 列出如果能問對方的三個問題。
- [x] 說明 AI 協助了哪些部分，以及人工調整了什麼。
- [x] 說明目前最不確定的程式碼段落與原因。

## 可列為後續優化

- [ ] 真正送出 API，因目前後端未提供。
- [ ] PDF 預覽、文字框選或來源高亮。
- [ ] 大量欄位虛擬列表最佳化。
- [ ] 草稿保存與恢復。
- [ ] 更完整的檔案大小與格式驗證。
- [ ] 解析逾時策略。
- [ ] 更完整的無障礙與跨裝置測試。